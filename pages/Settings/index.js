import React, { Component } from "react";
import { Text, View } from "react-native";
import { ListItem } from "react-native-elements";

class Settings extends Component {
  render() {
    return <ListItem title="Caisse" />;
  }
}

export default Settings;
